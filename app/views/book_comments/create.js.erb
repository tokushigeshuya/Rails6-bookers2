<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$(".comments_index").html("<%= j(render 'book_comments/index', book: @comment.book) %>");
$(".comments_area").html("<%= j(render 'book_comments/form', book: @comment.book, book_comment: @comment) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');